<h1>UPurchaseDetail#index</h1>
<table class="table ">
	  <thead class="thead-light">
	<tr><TH>Compra</TH><TH>Tipo</TH><TH>Cantidad</TH><TH>Ancho</TH><TH>Alto</TH><TH>Espesor</TH><TH>Costo</TH><TH>Subtotal</TH><TH>Observación</TH><TH colspan="2"><%= link_to 'Nuevo', u_purchase_detail_new_path %></TH></tr>
</thead>

	<tbody>
	<% @u_purchase_details.each do |f| %>
	<tr><TD><%= f.purchase_id %></TD>
		<TD><%= f.utilities_type.description %></TD>
		<TD><%= f.quantity %></TD>
		<TD><%= f.width %></TD>
		<TD><%= f.high %></TD>
		<TD><%= f.thickness %></TD>
		<TD><%= f.cost %></TD>
		<TD><%= f.subtotal %></TD>
		<TD><%= f.observation %></TD>
		<TD><%= link_to 'Editar', u_purchase_detail_edit_path(f['id']) %></TD><TD>

<%= link_to 'Borrar', { :action => :destroy, :id => f.id }, :method => :delete, :data => { :confirm => 'Desea borrar el registro?' } %>
		</TD>
	</tr>
    <% end %>
    </tbody>
</table>
</div>
