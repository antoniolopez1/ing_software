<%= form_with(model: utilities_type, local: true) do |form| %>
  <% if utilities_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utilities_type.errors.count, "error") %> prohibited this utilities_type from being saved:</h2>

      <ul>
      <% utilities_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Descripción %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
